#Hours have 3600 second 
duration = int (input ("Enter the duration in secounds :\n"))
# hours = seconds // 3600
# minutes = (seconds // 3600) // 60
# seconds = 
print (f"The duration is : {duration // 3600} hours + {duration % 3600 //60 } minutes + {duration % 60 } seconds")

print  (f"{1*30}")